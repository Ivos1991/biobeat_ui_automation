[pytest]
pythonpath = .
testpaths = tests
addopts = -ra --strict-markers -p no:cacheprovider
markers =
    ui: UI tests that run through the BioBeat web application.
    smoke: Focused smoke coverage for basic login and navigation paths.
    collect_all_evidence: Always attach the configured Playwright evidence set to Allure for this test.
